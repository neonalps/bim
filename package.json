{
  "name": "bim",
  "version": "1.0.0",
  "description": "Simple tool to track the departures of your public transport home stop",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && tsc && tsc-alias",
    "start": "npm run build && node build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "neonalps",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "tsc-alias": "^1.8.7",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "@fastify/cors": "^8.3.0",
    "axios": "^1.4.0",
    "dotenv": "^16.3.1",
    "env-var": "^7.3.1",
    "fastify": "^4.19.2",
    "pino": "^8.14.1",
    "undici": "^5.22.1"
  }
}
